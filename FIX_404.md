# Fix 404 Error on Netlify

## ✅ Configuration Fixed

I've updated the Netlify configuration. Now check your Netlify dashboard:

## 🔧 Check Netlify Dashboard Settings

1. **Go to Netlify Dashboard:**
   - Visit: https://app.netlify.com
   - Select your site: `thecoinshack`

2. **Verify Build Settings:**
   - Go to: **Site settings** → **Build & deploy** → **Build settings**
   - **Build command:** `npm run build`
   - **Publish directory:** Leave **EMPTY** (the plugin handles it)
   - Or set to: `.next` if the field is required
   - **Base directory:** Leave empty
   - **Node version:** `20`

3. **Verify Plugin:**
   - Go to: **Site settings** → **Plugins**
   - Make sure `@netlify/plugin-nextjs` is installed
   - If not, click "Install plugin" and search for "Next.js"

4. **Trigger New Deploy:**
   - Go to **Deploys** tab
   - Click **"Trigger deploy"** → **"Clear cache and deploy site"**

## 🚀 Alternative: Fix via Netlify Dashboard

If the above doesn't work, update build settings manually:

1. **Site settings** → **Build & deploy** → **Build settings**
2. **Edit settings** and ensure:
   ```
   Build command: npm run build
   Publish directory: (leave empty or .next)
   ```
3. **Save**
4. **Trigger new deployment**

## ✅ Expected Behavior

After the fix:
- ✅ Home page should load
- ✅ All routes should work
- ✅ No more 404 errors

---

**The configuration has been fixed. Update Netlify dashboard settings and redeploy!**

